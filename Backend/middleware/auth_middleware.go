package middleware

// The JWT auth middleware (which reads the Authorization header, verifies the
// token, and blocks unauthenticated requests) will live here. Left empty for
// now — it's needed to PROTECT routes like expenses, not for login/signup
// themselves, so it comes next.
